buildscript {
    ext {
        buildToolsVersion = "34.0.0"
        minSdkVersion = 23
        compileSdkVersion = 34
        targetSdkVersion = 34
        ndkVersion = "26.1.10909125"
        kotlinVersion = "1.9.22"
        RNMapboxMapsVersion = '11.0.0'
        //   RNMapboxMapsImpl = "mapbox"
        //  RNMapboxMapsImpl = "maplibre"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
    }
}

allprojects {
    repositories {
         maven { url 'https://jitpack.io' }
         maven{
            url "https://maven.pkg.jetbrains.space/data2viz/p/maven/dev"
        }
        maven{
            url "https://maven.pkg.jetbrains.space/data2viz/p/maven/public"
        }
        maven {
            url 'https://api.mapbox.com/downloads/v2/releases/maven'
            authentication {
                basic(BasicAuthentication)
            }
            credentials {
                // Do not change the username below.
                // This should always be `mapbox` (not your username).
                username = 'mapbox'
                // Use the secret token you stored in gradle.properties as the password
                // password = project.properties['MAPBOX_DOWNLOADS_TOKEN'] ?: ""
                password = 'sk.eyJ1IjoicXVpdHJvbmduZ3V5ZW4iLCJhIjoiY2x6ODNrcXFkMGdrcjJub3F4bjIxMjZoayJ9.i7WUq9Wth9NLMdVNbLCR0Q'
            }
        }
    }
}

apply plugin: "com.facebook.react.rootproject"
